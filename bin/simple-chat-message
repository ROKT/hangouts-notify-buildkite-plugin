#!/bin/bash
set -euo pipefail

# jq will read from stdin here
jq --compact-output --raw-input '{text: .}' \
    | curl --verbose --location --request POST "${1}" \
           --header 'Content-Type: application/json' \
           --data @-
