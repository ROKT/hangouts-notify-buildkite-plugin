#!/bin/bash
set -euo pipefail

# jq will read from stdin here
jq --compact-output . \
    | curl --verbose --location --request POST "${1}" \
           --header 'Content-Type: application/json' \
           --data @-
